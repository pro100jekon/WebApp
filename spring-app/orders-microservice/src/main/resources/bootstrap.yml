server:
  port: 8083

spring:
  application:
    name: orders-ms
  cloud:
    config:
      enabled: false
    discovery:
      enabled: true

eureka:
  client:
    enabled: true
    service-url:
      default: ${EUREKA_URL:http://localhost:8761}
